# Riven Configuration
# This file is gitignored - add local overrides to config_local.yaml
#
# Cores are loaded from the cores/ folder
# Each .yaml file in cores/ defines a core

# Default core to use
default_core: code_hammer

# Memory API settings
memory_api:
  url: "$MEMORY_API_URL"
  db_name: "riven"